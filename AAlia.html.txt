<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>For Aalia ❤️</title>
    <style>
        body {
            margin: 0; padding: 0; display: flex; justify-content: center;
            align-items: center; height: 100vh; background-color: #fff0f3;
            font-family: 'Segoe UI', sans-serif; overflow: hidden; text-align: center;
        }
        .container {
            z-index: 10; background: rgba(255, 255, 255, 0.9);
            padding: 50px; border-radius: 30px; border: 1px solid #ffccd5;
            max-width: 400px; box-shadow: 0 15px 35px rgba(255, 75, 110, 0.1);
        }
        h1 { color: #ff4d6d; font-size: 2.5rem; }
        p { color: #590d22; font-size: 1.1rem; margin-bottom: 30px; }
        .buttons { display: flex; justify-content: center; gap: 15px; }
        button {
            padding: 12px 30px; font-size: 1rem; border: none;
            border-radius: 12px; cursor: pointer; transition: 0.3s; font-weight: 600;
        }
        #yesBtn { background-color: #ff4d6d; color: white; }
        #noBtn { background-color: #f8f9fa; color: #a4133c; border: 1px solid #ffccd5; }
        button:hover { transform: translateY(-3px); opacity: 0.9; }
        .heart {
            position: absolute; color: #ffb3c1; pointer-events: none;
            z-index: 1; animation: float 6s linear infinite;
        }
        @keyframes float {
            0% { transform: translateY(105vh) scale(1); opacity: 1; }
            100% { transform: translateY(-10vh) scale(1.5); opacity: 0; }
        }
        .hidden { display: none; }
    </style>
</head>
<body>

    <div class="container" id="mainCard">
        <div id="askSection">
            <h1>Hi Aalia... ❤️</h1>
            <p>Will you be my Valentine?</p>
            <div class="buttons">
                <form id="valentineForm">
                    <input type="hidden" name="access_key" value="0d76fd6d-4b31-4ac9-983e-b673770a0269">
                    <input type="hidden" name="subject" value="Aalia answered your Valentine request!">
                    <input type="hidden" name="answer" id="answerInput" value="">
                    
                    <button type="button" id="yesBtn" onclick="sendResponse('Yes')">Yes</button>
                    <button type="button" id="noBtn" onclick="sendResponse('No')">No</button>
                </form>
            </div>
        </div>

        <div id="statusSection" class="hidden">
            <p id="statusText">Sending your answer...</p>
        </div>
    </div>

    <script>
        function sendResponse(answer) {
            const form = document.getElementById('valentineForm');
            const askSection = document.getElementById('askSection');
            const statusSection = document.getElementById('statusSection');
            const statusText = document.getElementById('statusText');
            
            // Set the value of the answer in the form
            document.getElementById('answerInput').value = answer;
            
            // Show loading state
            askSection.classList.add('hidden');
            statusSection.classList.remove('hidden');

            const formData = new FormData(form);
            const object = Object.fromEntries(formData);
            const json = JSON.stringify(object);

            fetch('https://api.web3forms.com/submit', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json', 'Accept': 'application/json' },
                body: json
            })
            .then(async (response) => {
                if (response.status == 200) {
                    if (answer === 'Yes') {
                        statusText.innerHTML = "<h1>Yay! ❤️</h1><p>I can't wait, Aalia!</p>";
                    } else {
                        statusText.innerHTML = "<h1>I understand. ❤️</h1><p>You'll always be special to me.</p>";
                    }
                } else {
                    statusText.innerHTML = "Something went wrong, but I still adore you!";
                }
            })
            .catch(error => {
                statusText.innerHTML = "Error sending response.";
            });
        }

        // Background Hearts
        function createHeart() {
            const heart = document.createElement('div');
            heart.classList.add('heart');
            heart.innerHTML = '❤️';
            heart.style.left = Math.random() * 100 + 'vw';
            heart.style.animationDuration = (Math.random() * 3 + 3) + 's';
            document.body.appendChild(heart);
            setTimeout(() => heart.remove(), 6000);
        }
        setInterval(createHeart, 400);
    </script>
</body>
</html>